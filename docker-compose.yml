version: '3'
services:
  api:
    image: hackerbay-backend
    build: .
    networks:
      - backend
    ports:
      - "3001:3001"

  web-cli:
    image: hackerbay-backend
    networks:
      - backend
    command: sh

networks:
  backend:
    driver: bridge